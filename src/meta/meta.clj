(ns meta.meta)

;; Set up Tranches in a batch

;; Run batch to get performance data
;;   Each tranche correlates with one backtest

;; Each batch will output correlations between metaparameters and performance

